name: RMCP lock refresh candidate

on:
  pull_request:
    branches: [dev]
    paths:
      - Cargo.toml
      - Cargo.lock
      - .github/workflows/rmcp-lock-candidate.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rmcp-lock-candidate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  candidate:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Check out exact candidate
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
          show-progress: false

      - name: Verify exact candidate
        env:
          EXPECTED_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"

      - name: Install pinned Rust toolchain
        run: rustup toolchain install 1.88.0 --profile minimal --component clippy,rustfmt

      - name: Generate the RMCP 3.2.0 lock candidate
        run: cargo +1.88.0 update -p rmcp --precise 3.2.0

      - name: Verify the dependency closure
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import re

          text = Path("Cargo.lock").read_text(encoding="utf-8")
          packages = {}
          for block in text.split("[[package]]")[1:]:
              name = re.search(r'^name = "([^"]+)"$', block, re.MULTILINE)
              version = re.search(r'^version = "([^"]+)"$', block, re.MULTILINE)
              if name and version:
                  packages.setdefault(name.group(1), set()).add(version.group(1))

          assert packages.get("rmcp") == {"3.2.0"}, packages.get("rmcp")
          assert packages.get("rmcp-macros") == {"3.2.0"}, packages.get("rmcp-macros")
          assert "chacha20" not in packages, packages.get("chacha20")
          PY
          cargo +1.88.0 metadata --locked --format-version 1 --no-deps >/dev/null
          cargo +1.88.0 tree --locked -p rmcp@3.2.0 -e normal > "${RUNNER_TEMP}/rmcp-tree.txt"

      - name: Preserve the reviewed manifest and authority workflow
        run: |
          git diff --exit-code -- Cargo.toml
          git diff --exit-code -- .github/workflows/resident-contract-admission.yml

      - name: Check formatting
        run: cargo +1.88.0 fmt --all --check

      - name: Lint the refreshed closure
        run: cargo +1.88.0 clippy --locked --all-targets --all-features -- -D warnings

      - name: Test protocol and security boundaries
        run: cargo +1.88.0 test --locked --all-targets --all-features

      - name: Build release
        run: cargo +1.88.0 build --locked --release

      - name: Install pinned RustSec scanner
        run: cargo +1.88.0 install cargo-audit --version 0.22.2 --locked

      - name: Audit the refreshed lock
        run: cargo +1.88.0 audit --deny warnings | tee "${RUNNER_TEMP}/cargo-audit.txt"

      - name: Retain the exact generated lock candidate
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v4.4.3
        with:
          name: rmcp-lock-candidate-${{ github.event.pull_request.head.sha || github.sha }}
          path: |
            Cargo.lock
            ${{ runner.temp }}/rmcp-tree.txt
            ${{ runner.temp }}/cargo-audit.txt
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 14
