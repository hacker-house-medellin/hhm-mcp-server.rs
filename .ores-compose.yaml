schema_version: ores.compose.v1
project: hhm-mcp-server
allow_lazy_start: false

services:
  hhm-mcp-server:
    name: hhm-mcp-server
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/hhm-mcp-server.rs"]
