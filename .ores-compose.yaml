schema_version: ores.compose.v1
project: hhm-mcp-server.rs
allow_lazy_start: false

services:
  hhm-mcp-server.rs:
    name: hhm-mcp-server.rs
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/hhm-mcp-server.rs"]
